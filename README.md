# homebrew-tap

Homebrew formulae for my own tools.

```bash
brew install christophecollet78/tap/rtk-meter
```

| Formula | Description |
| --- | --- |
| [`rtk-meter`](Formula/rtk-meter.rb) | macOS menu bar app showing [rtk](https://www.rtk-ai.app) token-savings stats |

Formulae here build from source, so nothing is quarantined by Gatekeeper.
